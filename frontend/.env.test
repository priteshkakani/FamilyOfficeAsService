# Test environment variables - Never commit secrets to version control!
# These should be set in your CI environment

# Supabase
VITE_SUPABASE_URL=https://your-test-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-test-anon-key

# For seeding/teardown scripts (only used in test environment)
SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# Test User Credentials
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=Test@1234

# Test Storage Paths
TEST_STORAGE_BUCKET=test-documents
TEST_STORAGE_PATH=test-uploads/
